# Copia este archivo a docker-compose.override.yml (está en .gitignore)
# y ajusta los valores locales.
version: "3.9"
services:
  app:
    environment:
      SPRING_PROFILES_ACTIVE: local
      SPRING_DEVTOOLS_RESTART_ENABLED: "true"
    volumes:
      - ./target:/app/target
